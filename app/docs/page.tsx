import Link from "next/link";
import { PageShell } from "../components/PageShell";
const docs=["Getting started","Installing FAST Launcher","Accounts and licences","Creating a match","Live coding workflow","Publishing to FAST Viewer","Managing users and roles","Troubleshooting"];
export default function Docs(){return <PageShell><section className="page-hero"><p className="eyebrow">Documentation</p><h1>Clear guidance.<br/><span>From setup to matchday.</span></h1><p className="lead">The public FAST knowledge base is being prepared alongside the platform. These sections show the documentation structure planned for launch.</p></section><section className="content-section docs-layout"><aside><strong>Documentation</strong>{docs.map((doc,index)=><a href={`#doc-${index}`} key={doc}>{doc}</a>)}</aside><div className="docs-content"><p className="eyebrow">Documentation preview</p><h2>Everything your team needs to get started.</h2><p>Setup guides, product workflows and troubleshooting content will be published here as FAST moves through private beta.</p>{docs.map((doc,index)=><article id={`doc-${index}`} key={doc}><span>{String(index+1).padStart(2,"0")}</span><div><h3>{doc}</h3><p>This guide will provide step-by-step instructions, screenshots and product-specific notes for {doc.toLowerCase()}.</p></div></article>)}<Link className="button button-primary" href="/contact">Ask a product question</Link></div></section></PageShell>}
