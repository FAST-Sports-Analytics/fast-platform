import Link from "next/link";
import { PageShell } from "../components/PageShell";
import { CTA } from "../components/CTA";
import { capabilities, products } from "../components/site-data";

export default function Platform(){return <PageShell><section className="page-hero"><p className="eyebrow">The FAST platform</p><h1>One workflow.<br/><span>Five connected products.</span></h1><p className="lead">Analyse matches, deliver selected insight, manage access and extend your workflow into scouting and assisted intelligence without stitching together disconnected tools.</p></section><section className="content-section"><div className="product-stack">{products.map(product=><Link href={`/platform/${product.slug}`} className="stack-row" key={product.slug}><span>{product.number}</span><div><small>{product.status}</small><h2>{product.name}</h2><p>{product.description}</p></div><b>↗</b></Link>)}</div></section><section className="content-section soft-section"><div className="section-heading compact"><p className="eyebrow">Platform capabilities</p><h2>Structured around the work your team already does.</h2></div><div className="feature-cards">{capabilities.map(([title,text],index)=><article key={title}><span>{String(index+1).padStart(2,"0")}</span><h3>{title}</h3><p>{text}</p></article>)}</div></section><CTA /></PageShell>}
